## JsonViewer

```jsx
import { JsonViewer } from '@authink/bottlejs'

export default () => <JsonViewer title="list" data={[1, 2, 3, 4]} />
```

```jsx
import { JsonViewer } from '@authink/bottlejs'

export default () => (
  <JsonViewer title="body" data={{ id: 100001, resetSecret: true }} />
)
```
